(*Объявление переменных и массивов*)

(*Повторное объявление переменных*)
$300,300
routine R[string s;integer t,s,t]
	(*Это имя уже занято*)
	$300
	[real SystemTime](input A)

initial

$300,300
string a,s,t;
boolean b;
(*Допустимый контекст объявления*)
if b then
	integer we,wr;
	while b do
		string hyu;
		for t:=1 by 1 to 10 do
			bit bi;
		endf
	endw
endif;

(*Использование необъявленных переменных + подавление повторной ошибки*)
$301,301
a:=a+bo+bo+c;

(*Нецелый индекс в объявлении массива + дублирование имени*)
$155,155,156,300
array [2,2.3,2+t/3] of char a;

(*Размерности массива не константы*)
$156,156
array [2,t+1,2+t*t] of bit n;

(*Размерности массива должны быть положительны*)
$171,171
array [-2,1,2-2-1*3,1] of bit o;

(*Тип массива должен быть простым*)
$201
array [1] of array [0] of char u;

(*Тип идекса должен быть целым*)
array [3,3] of char arrr;
$155,155,155
arrr[arrr[1,s],'2']:='c';

(*Выходы за границы*)
array [1+2*3,1,2*1+1+1] of integer arr;
arr[0,0,0]:=0;
$169
arr[6,-1,3]:=0;
$160,160,155
arr[arr[6,t-2,4],1,hyu]:=0;

(*Число индексов*)
$158
arr[1,0]:=0;

$317
arr[1,0,0,0]:=0;

(*Отличие массивов от переменных*)
$308
arr := 0;

$157
s[0]:="0";


endi
event;
(*Недопустимый контекст*)
$152
string str;

$152
case A: boolean bo
endc

ende

event E;
(*Недопустимый контекст*)
$152
string strd;

ende

endrout